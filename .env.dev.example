# ============================================
# infoSentry 开发环境配置示例
# ============================================
# 使用方法：
#   1. 复制此文件为 .env.dev: cp .env.dev.example .env.dev
#   2. 根据需要修改配置值
#   3. 启动服务时加载配置: docker-compose -f docker-compose.dev.yml --env-file .env.dev up -d
#
# 注意：开发环境已提供合理的默认值，大多数配置可以不用修改

# ============================================
# 数据库配置
# ============================================
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=infosentry
POSTGRES_PORT=5432

# ============================================
# Redis 配置
# ============================================
REDIS_PORT=6379

# ============================================
# 应用配置
# ============================================
# API 服务端口
API_PORT=8000

# Web 前端端口
WEB_PORT=3000

# 密钥（开发环境使用默认值即可）
SECRET_KEY=dev-secret-key-please-change-in-production

# 服务地址
FRONTEND_HOST=http://localhost:3000
BACKEND_HOST=http://localhost:8000

# ============================================
# OpenAI 配置（可选）
# ============================================
# 如果需要测试 AI 功能，填写以下配置
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1
OPENAI_EMBED_MODEL=text-embedding-3-small
OPENAI_JUDGE_MODEL=gpt-4o-mini

# ============================================
# 功能开关
# ============================================
# 是否启用 LLM 功能
LLM_ENABLED=true

# 是否启用向量嵌入
EMBEDDING_ENABLED=true

# 是否启用即时处理
IMMEDIATE_ENABLED=true

# 是否启用邮件发送（开发环境默认关闭）
EMAIL_ENABLED=false

# ============================================
# 邮件配置（可选）
# ============================================
# 如果需要测试邮件功能，填写以下配置
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAILS_FROM_EMAIL=
EMAILS_FROM_NAME=infoSentry Dev

# ============================================
# 速率限制和预算（开发环境较宽松）
# ============================================
# 每日 USD 预算
DAILY_USD_BUDGET=10.0

# 每日判断次数
JUDGE_PER_DAY=1000

# 每分钟嵌入次数
EMBED_PER_MIN=1000

# ============================================
# 日志配置
# ============================================
LOG_LEVEL=DEBUG

# ============================================
# 开发工具端口（启用 --profile tools 时使用）
# ============================================
# Redis UI 端口
REDIS_UI_PORT=8081

# PgAdmin 端口
PGADMIN_PORT=5050
PGADMIN_EMAIL=admin@infosentry.local
PGADMIN_PASSWORD=admin

# Flower (Celery 监控) 端口
FLOWER_PORT=5555
