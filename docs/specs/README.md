# docs/specs — Moved

Spec documents have been consolidated to the top-level `specs/` directory.

See: `../../specs/`

This directory is kept for backward compatibility during transition.
Canonical source is now `specs/`.
