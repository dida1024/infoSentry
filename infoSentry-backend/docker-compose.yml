# ============================================
# infoSentry Backend 本地开发环境
# ============================================
# 使用方法:
#   启动所有服务: docker-compose up -d
#   停止所有服务: docker-compose down
#   查看日志: docker-compose logs -f
#   重建服务: docker-compose up -d --build

services:
  # PostgreSQL with pgvector
  postgres:
    image: pgvector/pgvector:pg16
    container_name: infosentry-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: infosentry
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7-alpine
    container_name: infosentry-redis
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 5

  # API Server (可选，开发时通常直接本地运行)
  # api:
  #   build: .
  #   container_name: infosentry-api
  #   command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
  #   ports:
  #     - "8000:8000"
  #   environment:
  #     - POSTGRES_SERVER=postgres
  #     - POSTGRES_PORT=5432
  #     - POSTGRES_USER=postgres
  #     - POSTGRES_PASSWORD=postgres
  #     - POSTGRES_DB=infosentry
  #     - REDIS_URL=redis://redis:6379/0
  #     - ENVIRONMENT=local
  #   volumes:
  #     - .:/app
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     redis:
  #       condition: service_healthy

  # Worker - Ingest (可选)
  # worker_ingest:
  #   build: .
  #   container_name: infosentry-worker-ingest
  #   command: celery -A src.core.infrastructure.celery.app worker -Q q_ingest -c 2 -l INFO
  #   environment:
  #     - POSTGRES_SERVER=postgres
  #     - REDIS_URL=redis://redis:6379/0
  #   depends_on:
  #     - postgres
  #     - redis

  # Worker - Embed & Match (可选)
  # worker_embed_match:
  #   build: .
  #   container_name: infosentry-worker-embed-match
  #   command: celery -A src.core.infrastructure.celery.app worker -Q q_embed,q_match -c 1 -l INFO
  #   environment:
  #     - POSTGRES_SERVER=postgres
  #     - REDIS_URL=redis://redis:6379/0
  #   depends_on:
  #     - postgres
  #     - redis

  # Worker - Agent (可选)
  # worker_agent:
  #   build: .
  #   container_name: infosentry-worker-agent
  #   command: celery -A src.core.infrastructure.celery.app worker -Q q_agent -c 1 -l INFO
  #   environment:
  #     - POSTGRES_SERVER=postgres
  #     - REDIS_URL=redis://redis:6379/0
  #   depends_on:
  #     - postgres
  #     - redis

  # Worker - Email (可选)
  # worker_email:
  #   build: .
  #   container_name: infosentry-worker-email
  #   command: celery -A src.core.infrastructure.celery.app worker -Q q_email -c 1 -l INFO
  #   environment:
  #     - POSTGRES_SERVER=postgres
  #     - REDIS_URL=redis://redis:6379/0
  #   depends_on:
  #     - postgres
  #     - redis

  # Celery Beat (可选)
  # beat:
  #   build: .
  #   container_name: infosentry-beat
  #   command: celery -A src.core.infrastructure.celery.app beat -l INFO
  #   environment:
  #     - POSTGRES_SERVER=postgres
  #     - REDIS_URL=redis://redis:6379/0
  #   depends_on:
  #     - postgres
  #     - redis

volumes:
  postgres_data:
  redis_data:

